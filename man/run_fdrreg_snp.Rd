% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_fdrreg_snp.r
\name{run_fdrreg_snp}
\alias{run_fdrreg_snp}
\title{Run FDRreg at the SNP Level}
\usage{
run_fdrreg_snp(
  target,
  aux = list(),
  overlap_traits = NULL,
  cov_matrix = NULL,
  ldsc_intercepts = NULL,
  target_name = "target",
  annotations = NULL,
  id_col = "snpid",
  z_col = "z",
  p_col = "pval",
  ldsc_col = list(trait1 = "trait1", trait2 = "trait2", intercept = "intercept"),
  feature_transform = c("signed", "abs", "split"),
  var_select = c("none", "lasso", "marginal", "elasticnet"),
  fdrreg_nulltype = c("both", "theoretical", "empirical"),
  fdrreg_method = "pr",
  seed = 42
)
}
\arguments{
\item{target}{Data frame of target GWAS. Must contain columns
specified by \code{id_col}, \code{z_col}, \code{p_col}.}

\item{aux}{Named list of data frames (auxiliary GWAS).}

\item{overlap_traits}{Character vector of names in \code{aux} that
have sample overlap with the target.}

\item{cov_matrix}{Pre-built covariance matrix. If NULL and
\code{ldsc_intercepts} is provided, built from LDSC.}

\item{ldsc_intercepts}{Data frame with LDSC intercept values.}

\item{target_name}{Character, target trait name (default "target").}

\item{annotations}{Data frame of biological annotations.}

\item{id_col}{Character, SNP ID column (default "snpid").}

\item{z_col}{Character, z-score column (default "z").}

\item{p_col}{Character, p-value column (default "pval").}

\item{ldsc_col}{Named list: trait1, trait2, intercept column names.}

\item{feature_transform}{Character, "signed" (default), "abs", or "split".}

\item{var_select}{Character, "none" (default), "lasso", "marginal",
or "elasticnet".}

\item{fdrreg_nulltype}{Character, "both" (default), "theoretical",
or "empirical".}

\item{fdrreg_method}{Character, passed to FDRreg (default "pr").}

\item{seed}{Integer, random seed (default 42).}
}
\value{
An object of class \code{fdrreg_result}.
}
\description{
Performs FDRreg on SNP-level GWAS summary statistics with optional
sample-overlap decorrelation, biological annotations, and variable selection.
}
\examples{
\dontrun{
sim <- simulate_example_data(n_snps = 2000, seed = 42)
result <- run_fdrreg_snp(
  target = sim$snp_target,
  aux = sim$snp_aux,
  overlap_traits = sim$overlap_traits,
  ldsc_intercepts = sim$ldsc_intercepts
)
print(result)
significant(result, threshold = 0.05)
}

}
