% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotations.r
\name{prepare_custom_annotations}
\alias{prepare_custom_annotations}
\title{Prepare and Combine Custom User Annotations}
\usage{
prepare_custom_annotations(
  user_annotations,
  id_col = "ID",
  base_annotations = NULL,
  base_id_col = NULL,
  fill_na = 0
)
}
\arguments{
\item{user_annotations}{A data frame containing custom annotations.}

\item{id_col}{Character, name of the identifier column in \code{user_annotations}. Default is "ID".}

\item{base_annotations}{Optional data frame of existing/baseline annotations to merge with.}

\item{base_id_col}{Character, name of the identifier column in \code{base_annotations}. Defaults to \code{id_col}.}

\item{fill_na}{Numeric value to replace missing values (NA/Inf) with. Default is 0.}
}
\value{
A processed data frame with the ID column and numeric feature columns, ready for FDRreg.
}
\description{
Provides a flexible helper to merge, format, and validate user-supplied
annotation data (e.g., custom pathway scores, single-cell markers, epigenetic peaks)
with optional baseline annotations.
}
\examples{
custom_df <- data.frame(
  ID = c(1, 2, 9, 10),
  pathway_score = c(1.2, 0.5, 3.1, 0.0),
  is_hub = c(1, 0, 1, 0)
)
prep_annot <- prepare_custom_annotations(custom_df, id_col = "ID")
head(prep_annot)
}
